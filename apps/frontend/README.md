# Frontend

Next.js dashboard for DPIN Uptime local MVP.

Routes:
- `/` landing
- `/login`
- `/register`
- `/dashboard`

Requires API running at `NEXT_PUBLIC_API_URL` (default `http://localhost:3001/v1`).

Run:

```bash
bun run dev
```
