replicaCount: 2

image:
  repository: ghcr.io/phantomsoldierking/dpin-frontend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

env:
  NEXT_PUBLIC_API_URL: https://api.example.com/v1
